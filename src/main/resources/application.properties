spring.application.name=jwt_example
jwt.secretKey=json web token secret key padded to get 256 bytes long
jwt.role_user=ROLE_USER
jwt.id=JWT_id
jwt.header=authorization
jwt.prefix=Bearer 
jwt.claim.authorities=authorities
# 1 day: 24*60*60*1000 = 86400000
jwt.expiration_ms_to_add=86400000